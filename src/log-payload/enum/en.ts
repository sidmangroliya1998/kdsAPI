export enum CommonActionType {
  CREATE = 'create',
  UPDATE = 'update',
  DELETE = 'delete',
}
export enum CollectionType {
  cashiers = 'cashiers',
  orders = 'orders',
  cashierlogs = 'cashierlogs',
  tables = 'tables',
  tablelogs = 'tablelogs',
}
